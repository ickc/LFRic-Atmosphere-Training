.. Re-usable warning for "Other" tabs whose exercise needs a site HPC service.
.. To use it: .. include:: /include/other-platform-hpc.rst

.. warning::

   "Other" means your own machine, or a system run by another institution
   (see :ref:`platform-tabs`). This step cannot be completed there without
   local equivalents of the Met Office services it depends on: a supported
   LFRic build environment, repository access, shared ancillary data, and an
   HPC batch queue. The commands below are given so you can follow along and
   adapt them to your site, not because they will work unchanged.
